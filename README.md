# Dotfiles (vl-cr)

## Setup

```shell
bash install.sh
```

Optional dev tools (e.g. for Kubernetes, AWS, Python) can be installed with:

```shell
brew bundle install --file=Brewfile-dev --no-lock
```

## Todo

- Ignore directories with [fd & fzf](https://github.com/junegunn/fzf.vim/issues/453)
- Configure global Taskfile for miscellaneous util tasks
